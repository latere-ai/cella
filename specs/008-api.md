---
title: "API: the /v1 kinds, addressing and concurrency, streams, the error table, OpenAPI"
status: validated
track: core
depends_on:
  - specs/003-manifest-contract.md
  - specs/005-lifecycle-controller.md
  - specs/006-identity.md
  - specs/007-admission.md
  - specs/010-state.md
affects: [internal/api/, api/, internal/config/, docs/]
effort: large
created: 2026-09-12
updated: 2026-09-12
author: changkun
---

# API

## Overview

One HTTP surface on the public listener under `/v1`, one grammar for
every kind: apply by name, read and act by id, list, delete, the
kind's verbs, and for a sandbox the streams into it. Every response
body is JSON, or YAML on request, except a stream. Every error is one
envelope with one code and one fixed user sentence, and this spec is
the table those sentences live in. The OpenAPI document is generated
from the Go types of every kind and served, so a client generator and
the server agree by construction.

## Current state

Not built. The routes descend from the hosted platform's `/v1`, with
the manifest as the only create body and apply by name added.

## Design

### Addressing

Every kind is applied by name and addressed by id. `PUT
/v1/<kinds>/{name}` creates the caller's object of that name or
updates it; every other item route takes `{id}`, the prefixed ULID of
[[001-architecture]], and also accepts a name, resolved among the
objects whose `owner` is the caller's; for a workload caller, the
owner is its own `status.owner`, so a sandbox addresses its siblings
and children by name. A body whose `metadata.name` differs from the
path is `invalid_field` at `metadata.name`. `POST /v1/sandboxes` with a
name another live object of the caller's holds is `name_taken`;
without a name, `Resolve` generates one through `NewName`.

### Concurrency

Every object response carries `ETag: "<version>"` from the store. A
`PUT` may carry `If-Match` with that value; the handler resolves and
`Put`s with it, and a row that moved is 409 `version_conflict`. A
`PUT` without `If-Match` is a read-modify-write the handler retries up
to three times on a conflict before returning `version_conflict`, so a
caller that does not care about concurrency never sees one in
practice.

### Assembling Resolve

For every apply the handler builds `manifest.Options` as follows.
`Actor` is the rendered subject of [[006-identity]] and `Workload` is
true when the subject carries the `sandbox:` prefix. `Lookup` is
constructed per request over the store and the authorizer's decision
cache, asking `secret.mount`, `volume.attach`, and `environment.use`,
and mapping a deny to `not_found`. `Existing` is the caller's object
of that name when one exists. `Parent` is, for a workload caller, its
own sandbox's desired spec and status, loaded by the id in the
subject; a spawn is any `POST` or `PUT` by a workload. `Defaults` and
`Ceilings` are the operator's ([[007-admission]]); `Limits` carries
the authorizer's `max_priority` for this subject; `Admit` is the
configured admission function with an `AdmitRequest` carrying the
request id; `Now` is the clock; `NewName` is the API's generator. The
count ceiling is checked in the same transaction as the desired write
([[007-admission]]). The response is the resolved manifest with the
stored status merged in.

### Routes

Public documents, outside `/v1` and without a bearer: `GET
/.well-known/jwks.json` (the token key set) and `GET /openapi.yaml`.
Everything under `/v1` requires a bearer; the worker routes accept an
environment key and nothing else.

| Method | Path | Does | Action |
|---|---|---|---|
| `POST` | `/v1/sandboxes` | create from a manifest; 201 | `sandbox.create` |
| `PUT` | `/v1/sandboxes/{name}` | apply; 201 or 200 | `sandbox.create` or `sandbox.update` |
| `GET` | `/v1/sandboxes` | list; `?label=k=v` repeated (all must match), `?phase=`, `?owner=`, `?environment=`, `?root={id}` (a spawn tree), `?cursor=`, `?limit=` (default 50, at most 200; above is `invalid_field`) | `sandbox.list` |
| `GET` | `/v1/sandboxes/{id}` | the object with status | `sandbox.read` |
| `DELETE` | `/v1/sandboxes/{id}` | accepted in every phase; 202 with the object in `Deleting` | `sandbox.delete` |
| `POST` | `/v1/sandboxes/{id}/start`, `/stop` | the verbs; 200 with the object; `phase_conflict` as [[005-lifecycle-controller]] says | `sandbox.update` |
| `POST` | `/v1/sandboxes/{id}/exec` | JSON `{"command": [...], "env": {}, "workdir": "", "timeout": "10m"}`; the response is the exec stream below, or with `?wait=1` a JSON result; no stdin, no TTY | `sandbox.exec` |
| `GET` | `/v1/sandboxes/{id}/exec` | a WebSocket for a command with stdin or a TTY; needs `Attach` | `sandbox.exec` |
| `GET` | `/v1/sandboxes/{id}/attach` | a WebSocket carrying a PTY; needs `Attach` | `sandbox.exec` |
| `GET` | `/v1/sandboxes/{id}/dial/{port}` | a WebSocket carrying raw bytes to a port inside; needs `Dial` | `sandbox.exec` |
| `GET` | `/v1/sandboxes/{id}/files?path=...` (repeated) | a tar stream of the paths | `sandbox.exec` |
| `PUT` | `/v1/sandboxes/{id}/files?dest=...` | a tar stream extracted under `dest`; at most `CELLA_MAX_UPLOAD_BYTES` | `sandbox.exec` |
| `GET` | `/v1/sandboxes/{id}/logs?follow=1&since=<RFC 3339>&tail=<n>` | the main process output | `sandbox.read` |
| `GET` | `/v1/sandboxes/{id}/events?follow=1&cursor=&limit=` | the journal, newest first, or following | `sandbox.read` |
| `GET` | `/v1/events?object={id}&cursor=&limit=&follow=1` | any object's events, newest first or following; the handler derives the kind and authorizes its read ([[009-events]]) | `<kind>.read` |
| `GET` | `/v1/sandboxes/{id}/egress?cursor=&limit=` | the connections the gateway reported for this sandbox, newest first ([[018-egress-and-secrets]]) | `sandbox.read` |
| `POST` | `/v1/sandboxes/{id}/token` | mint a workload token; 201 `{"token", "exp"}` | `sandbox.token` |
| `GET` | `/v1/sandboxes/{id}/display` | geometry and `DisplayReady`; needs `Display` | `sandbox.read` |
| `GET` | `/v1/sandboxes/{id}/screenshot?format=png|jpeg&scale=` | one frame; needs `Display` | `sandbox.exec` |
| `GET` | `/v1/sandboxes/{id}/screen?fps=` | a WebSocket of frames; needs `Display` | `sandbox.exec` |
| `POST` | `/v1/sandboxes/{id}/input` | a batch of events; at most 1 MiB; needs `Input` | `sandbox.exec` |
| any | `/v1/sandboxes/{id}/ports/{name}/{path...}` | an HTTP proxy to a declared port; 502 `upstream_unavailable` when nothing listens; emits `sandbox.port` with `CELLA_EVENTS_PORTS=1` | `sandbox.exec` |
| `PUT`, `GET`, `DELETE` | `/v1/secrets/{name}`; `GET`, `DELETE /v1/secrets/{id}`; `GET /v1/secrets` | the `Secret` kind; `spec.value` never returned | `secret.create` or `.update`, `.read`, `.delete`, `.list` |
| `PUT`, `GET`, `DELETE` | `/v1/volumes/{name}`; `GET`, `DELETE /v1/volumes/{id}`; `GET /v1/volumes` | the `Volume` kind | `volume.*` as the verb |
| `POST`, `GET`, `DELETE` | `/v1/volumes/{id}/snapshots`, `/v1/volumes/{id}/snapshots/{snp_ id}` | take, list, or delete a crash-consistent snapshot; needs `Snapshots` | `volume.snapshot`, `volume.read`, `volume.snapshot` |
| `PUT`, `GET`, `DELETE` | `/v1/sandboxsets/{name}`; `GET`, `DELETE /v1/sandboxsets/{id}`; `GET /v1/sandboxsets`; `POST .../{id}/stop` | the `SandboxSet` kind; `GET` carries the replica table; `PUT` on an existing set updates `parallelism` and `onFailure` only | `set.create`, `.update`, `.read`, `.delete`, `.list` |
| `PUT`, `GET`, `DELETE` | `/v1/environments/{name}`; `GET`, `DELETE /v1/environments/{id}`; `GET /v1/environments` | the `Environment` kind | `environment.*` as the verb |
| `POST`, `DELETE` | `/v1/environments/{id}/keys`, `/v1/environments/{id}/keys/{jti}` | mint, shown once; revoke | `environment.key` |
| `GET` | `/v1/environments/{id}/operations` | the worker's WebSocket ([[021-data-plane-workers]]); environment key only | none beyond the key |
| `GET` | `/v1/environments/{id}/egress` | the gateway's WebSocket: maps down, connection records up ([[018-egress-and-secrets]]); environment key only | none beyond the key |
| `POST` | `/v1/environments/{id}/workers` | registration; environment key only | none beyond the key |

The handler order for every item route: read the object by id or
alias, build the authorizer's `resource` from it, authorize, act. A
missing object is `not_found`; a deny on the request's own action is
`forbidden`; a deny reached through `Lookup` at resolve is `not_found`
([[006-identity]]). `?owner=` and every other list selector are
intersected with the authorizer's `filter`, so a selector outside the
filter yields an empty list, never 403.

### Capability gates

A route that needs a capability the sandbox's environment does not
declare is 422 `capability_unsupported` before any driver call:
`Attach` for the exec WebSocket and attach, `Dial` for dial,
`Snapshots` for the snapshot routes, `Display` for display,
screenshot, and screen, `Input` for input, `Files` for the file routes
while the sandbox is `Stopped`.

### Bodies and content types

Manifest bodies accept the types of [[003-manifest-contract]], are
capped at `CELLA_MAX_BODY_BYTES` (413 `body_too_large`), and any other
type is 415 `unsupported_media_type`. Tar uploads are capped at
`CELLA_MAX_UPLOAD_BYTES` (default `1Gi`). Every object response is the
resolved manifest with `status`, as JSON in the Go struct order the
OpenAPI document records, or as YAML when `Accept` names one of the
three YAML types; any other `Accept` that excludes JSON is 406
`not_acceptable`. A list response is `{"items": [...], "next": "<cursor
or empty>"}`.

### Streams

| Stream | Transport | Framing |
|---|---|---|
| exec (`POST`) | the HTTP response, `Content-Type: application/vnd.cella.exec-stream` | frames of one byte of channel (`1` stdout, `2` stderr, `3` exit, `4` error) plus a 4-byte big-endian length at most 1 MiB plus the payload; the exit frame carries the code as decimal text and closes the stream; the error frame carries an error envelope as JSON and closes it |
| exec (`GET`), attach | WebSocket, subprotocol `cella.exec.v1` | the first text frame is the JSON request (`command`, `env`, `workdir`, `cols`, `rows`, `timeout`); binary frames are bytes both ways; text frames after the first are `{"resize": {"cols", "rows"}}` from the client and `{"exit": n}` or `{"error": {...}}` from the server, each followed by a close with code 1000 or 1011 |
| dial | WebSocket, `cella.dial.v1` | binary frames are bytes both ways; a close code 1000 when the inside closes, 1011 on error |
| files | tar, `Content-Type: application/x-tar` | the whole body; an error after the first byte is a truncated tar and a trailer header `X-Cella-Error` when the transport allows |
| logs, events with `follow` | newline-delimited, `application/x-ndjson` for events and `text/plain` for logs | one line per record; the connection closes with the sandbox |
| screen | WebSocket, `cella.screen.v1` | binary frames are PNG, at `?fps=` up to 10 |
| gateway sync | WebSocket, `cella.egress.v1` | one JSON text message per line: `hello`, `snapshot`, `put`, `purge`, `ack`, `heartbeat`, `record` ([[018-egress-and-secrets]]) |
| worker operations | WebSocket, `cella.worker.v1` | frames of a 26-byte operation id, one byte of sub-stream (`0` control, `1` stdin, `2` stdout, `3` stderr, `4` bytes), and the payload; control frames are JSON operations, results, and heartbeats; a reconnect resumes from the store's unacknowledged claims ([[010-state]]) |

The `POST` exec with `?wait=1` returns `{"exitCode": n, "stdout":
"...", "stderr": "...", "truncated": false, "durationMs": n}` with
stdout and stderr each capped at 1 MiB, the head kept and `truncated`
set; a `timeout` expiry ends the command and reports exit code 124.
Once a stream has begun, an error can no longer change the status
code, which is what the error frame and close code 1011 carry.

### The error table

The envelope is `latere.ai/x/pkg/httpjson`'s: `{"error": {"code",
"message", "details"}}`, where `details` always carries `request_id`,
carries `paths` (a list) for a code that names fields, and `detail`
for a developer sentence. `message` is the fixed user sentence below;
`TestErrorTable` asserts one per code and that no handler writes
another.

| Code | Status | Message |
|---|---|---|
| `unsupported_media_type` | 415 | Send the manifest as JSON or YAML. |
| `not_acceptable` | 406 | This endpoint answers in JSON or YAML. |
| `multi_document` | 400 | Send one manifest per request. |
| `unsupported_version` | 400 | This server serves cella.latere.ai/v1beta1. |
| `unsupported_kind` | 400 | This server does not serve that kind. |
| `unknown_field` | 400 | The manifest has a field this schema does not know. |
| `missing_field` | 400 | A required field is missing. |
| `invalid_field` | 400 | A field has a value it cannot take. |
| `reserved_prefix` | 400 | That name is reserved for the control plane. |
| `exclusive_fields` | 400 | Two fields that cannot be set together are set. |
| `path_conflict` | 400 | Two mounts share a path. |
| `bad_request` | 400 | The request could not be read. |
| `body_too_large` | 413 | The request body is larger than this server accepts. |
| `unauthenticated` | 401 | Sign in and send a valid token. |
| `forbidden` | 403 | You do not have permission to do this. |
| `not_found` | 404 | There is no such object. |
| `immutable_field` | 409 | This field cannot be changed after the object is created. |
| `boundary_widened` | 409 | A sandbox cannot widen its own boundary. |
| `name_taken` | 409 | You already have an object with this name. |
| `version_conflict` | 409 | The object changed since you read it; read it again and retry. |
| `phase_conflict` | 409 | The sandbox is not in a state that allows this. |
| `volume_busy` | 409 | The volume is attached in a way that excludes this. |
| `secret_host_conflict` | 409 | Two mounted secrets apply to the same host. |
| `secret_out_of_scope` | 422 | The secret does not cover the host it is used for. |
| `ceiling_exceeded` | 422 | The value is above what this server allows. |
| `capability_unsupported` | 422 | The environment cannot provide this. |
| `admission_refused` | 422 | The request was refused by this server's policy. |
| `quota_exceeded` | 422 | You have reached your sandbox limit. |
| `boundary_exceeded` | 422 | A child sandbox cannot exceed its parent's boundary. |
| `spawn_budget_exhausted` | 422 | The sandbox has no spawn budget left. |
| `rate_limited` | 429 | Too many requests; wait and retry. |
| `admission_unavailable` | 503 | The policy service is unavailable; retry shortly. |
| `authorizer_unavailable` | 503 | The permission service is unavailable; retry shortly. |
| `driver_unavailable` | 503 | The environment is unavailable; retry shortly. |
| `upstream_unavailable` | 502 | Nothing is listening on that port. |

### Rate limit

After authentication, `CELLA_REQUESTS_PER_MINUTE` per subject, the
authorizer's `limits.requests_per_minute` overriding it, a token
bucket per replica with the limit as the burst; `0` disables. Streams
count once at open. Before authentication, one bucket per client
address of `CELLA_UNAUTHENTICATED_REQUESTS_PER_MINUTE` (default `60`)
bounds a flood of bad tokens. Every response carries
`RateLimit-Limit`, `RateLimit-Remaining`, and `RateLimit-Reset`; a 429
adds `Retry-After`.

### Request id

Every request has one, `req_` plus a ULID. A client's `X-Request-Id`
of at most 128 printable ASCII characters is accepted as is; anything
else is replaced. It is the `request_id` of the envelope, the
`request.id` the authorizer sees, `AdmitRequest.RequestID`, the
event's `request_id`, and the trace's correlation
([[017-observability]]).

### OpenAPI

`tools/apidoc` generates `api/openapi.yaml` from the types of every
kind, the route table, and the error table; the gate fails when the
committed file differs from the generated one, and the server
validates every request against it in the suite so a handler cannot
drift from the document. [[001-architecture]]'s
`TestAPIAndImporterResolveAgree` compares the `PUT` response body with
`Resolve`'s output after stripping `status` from both.

## Not in this spec

The command that speaks this API ([[011-agent-client]]); the
conformance suite that proves it ([[015-conformance-suite]]); the
frames' meaning per operation ([[021-data-plane-workers]],
[[023-computer-use-operations]]).

## Acceptance criteria

| Criterion | Test that proves it | State |
|---|---|---|
| Every `/v1` route in the table exists, requires a bearer, and asks the authorizer exactly the action in its row; the two public documents need none; the worker routes accept an environment key and refuse a caller token | `TestRouteTableActions`, one row per route and verb | not built |
| `PUT` twice with the same manifest is 201 then 200 and the object is unchanged; a stale `If-Match` is 409 `version_conflict`; without `If-Match` a concurrent write is retried and succeeds | `TestApplyConcurrency` | not built |
| A body whose name differs from the path is `invalid_field`; `POST` with a taken name is `name_taken`; `POST` without a name gets a generated one | `TestNamesOnApply` | not built |
| A name alias resolves under the caller's owner, and for a workload under its root's owner | `TestNameAlias` | not built |
| The handler reads, authorizes, then acts; a missing object is 404; a deny on the own action is 403; a deny through `Lookup` is 404 | `TestReadAuthorizeAct` | not built |
| A list pages through `items` and `next` at the default and maximum limits; `?limit=300` is `invalid_field`; repeated `?label=` is a conjunction; `?owner=` outside the authorizer's `filter` is an empty list; `?root=` returns the tree | `TestListSelectors` | not built |
| Every code in the table has its status and one fixed sentence, and no handler writes a sentence outside the table | `TestErrorTable` | not built |
| The envelope is `httpjson`'s with `request_id` always and `paths` for `immutable_field` naming every changed path | `TestEnvelope` | not built |
| The exec stream carries 64 MiB of stdout in bounded frames and ends with an exit frame; an error mid-stream is an error frame; `?wait=1` caps and marks truncation and reports 124 on timeout | `TestExecStream`, `TestExecWait` | not built |
| The exec and attach WebSockets carry bytes both ways, a resize reaches the PTY, and the exit arrives as a text frame before close 1000 | `TestAttachRoundTrip` | not built |
| Dial reaches a port inside; the port proxy forwards every method and answers 502 `upstream_unavailable` when nothing listens | `TestDialAndPortProxy` | not built |
| Each capability gate answers 422 before any driver call on an environment without the capability | `TestCapabilityGates`, table-driven | not built |
| Files round-trip a tree; an upload past the cap is 413; a manifest past its cap is 413; a wrong content type is 415; an `Accept` excluding JSON and YAML is 406 | `TestBodiesAndTypes` | not built |
| Logs honour `since`, `tail`, and `follow`; events page newest first and follow | `TestLogsAndEvents` | not built |
| `DELETE` is 202 in every phase; `start` on `Failed` is `phase_conflict`; `stop` keeps the workspace | `TestLifecycleVerbs` | not built |
| The 601st authenticated request in a minute is 429 with the four headers; the 61st bad token from one address is 429 | `TestRateLimits` | not built |
| A client request id within the rule is echoed everywhere; one outside it is replaced | `TestRequestId` | not built |
| The committed OpenAPI document equals the generated one and every request in the suite validates against it | `TestOpenAPIIsCurrent` | not built |
