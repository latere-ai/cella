{
  "note": "The cases this repository's own server fails today. Each names a route or a rule of the API design that is not built yet; the case stays as it is and the gap is closed in the server. A declared case that passes fails the run, so a line here cannot outlive its gap. The list is empty: this server answers every case the suite runs against it.",
  "cases": {}
}
