{
  "apiVersion": "cella.latere.ai/v1beta1",
  "kind": "Sandbox",
  "metadata": {"name": "with-a-secret"},
  "spec": {
    "command": ["/bin/sh", "-c", "sleep 60"],
    "secrets": [{"name": "vendor", "env": "VENDOR_TOKEN"}]
  }
}
